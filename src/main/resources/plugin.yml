name: rpDice
version: '${version}'
main: net.lordofthetimes.rpDice.RpDice
api-version: '1.21'
author: LordofTheTimes
dependencies:
  server:
    CommandAPI:
      load: BEFORE
      required: true
      join-classpath: true